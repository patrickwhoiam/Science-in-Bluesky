# ── Core pipeline (run_pipeline.py, hydrate_engagement_ids.py, collect_altmetrics.py) ──
pandas>=2.0
pyarrow>=14.0
PyYAML>=6.0
requests>=2.31
tqdm>=4.66
tabulate>=0.9

# ── Analysis notebooks (shared) ──
matplotlib>=3.8
numpy>=1.24
scipy>=1.11
jupyter>=1.0

# ── RQ1 — Dissemination Landscape ──
# (OpenAlex enrichment uses requests, already listed above)

# ── RQ2 — Engagement Dynamics ──
nltk>=3.8
vaderSentiment>=3.3.2
transformers>=4.40
torch>=2.0                  # GPU recommended for NLI classification (~60-90 min on RTX 3050)
                            # CPU fallback works but takes ~8-14 hours
scikit-learn>=1.3           # Cohen's kappa (validation, optional)

# ── RQ3 — Architectural Influence ──
networkx>=3.2
python-louvain>=0.16        # import: community

# ── Dataset overview notebook ──
# (no additional packages beyond above)
